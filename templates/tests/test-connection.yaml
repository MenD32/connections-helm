apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "connections-helm.fullname" . }}-test-connection"
  labels:
    {{- include "connections-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "connections-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
